export default class Tour {

  id = ``
  map = ``
  title = ``
  desc = ``

  constructor({id, map, title, desc}) {
    this.id = id;
    this.map = map;
    this.title = title;
    this.desc = desc;
  }

}
